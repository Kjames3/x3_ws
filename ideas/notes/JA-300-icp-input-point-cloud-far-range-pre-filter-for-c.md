---
id: JA-300
title: "ICP Input Point Cloud Far-Range Pre-Filter for Correspondence Matrix Reduction"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "300"
session: "[2026-06-09] Automated Analysis Batch — Ideas 293 to 302"
tags: [idea]
---

# JA-300 — ICP Input Point Cloud Far-Range Pre-Filter for Correspondence Matrix Reduction

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Navigation Accuracy
**Investment:** Extremely Low
**Rationale:** In `_align_scans_icp`, `P` and `Q` include all valid scan points up to 4.0 m (as filtered in `_scan_cb`). For corridor navigation, points at 2.0–4.0 m are typically distant walls that moved minimally between scan intervals, contributing large unmatched entries to the O(M×N) distance matrix that never pass the 0.25 m² correspondence threshold while consuming CPU in the inner loop. Adding `P = P[np.sum(P**2, axis=1) < 4.0]` and `Q = Q[np.sum(Q**2, axis=1) < 4.0]` (range ≤ 2.0 m filter) at the start of `_align_scans_icp` reduces matrix dimensions by 30–50% in typical corridor scans at no accuracy cost, since close-range wall points dominate all valid correspondences.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `300`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `300` in its own log, but `June_performance_ideas.md` also numbers a different idea `300`. Always use the namespaced id `JA-300`.

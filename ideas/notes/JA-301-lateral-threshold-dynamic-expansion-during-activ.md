---
id: JA-301
title: "`LATERAL_THRESHOLD` Dynamic Expansion During Active Bypass in `_get_speed_scaling`"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "301"
session: "[2026-06-09] Automated Analysis Batch — Ideas 293 to 302"
tags: [idea]
---

# JA-301 — `LATERAL_THRESHOLD` Dynamic Expansion During Active Bypass in `_get_speed_scaling`

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Collision Avoidance
**Investment:** Extremely Low
**Rationale:** In `_get_speed_scaling()`, `LATERAL_THRESHOLD = 0.35 m` defines the safety corridor half-width for proximity and TTC checks (line 727 of `ab_comparison_test.py`). During an active bypass with `target_lateral_offset = +0.25 m`, the robot drives offset from centerline; an obstacle at `ry = 0.42 m` sits directly in the bypass path but falls outside the static 0.35 m threshold and receives no TTC scaling. Computing `LATERAL_THRESHOLD = 0.35 + 0.5 * abs(self.target_lateral_offset)` when `self.target_lateral_offset != 0.0` expands the safety corridor to cover the actual swept path, closing the TTC blind spot that Idea 70's travel-frame projection does not address (it adjusts coordinate orientation but not threshold width).

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `301`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `301` in its own log, but `June_performance_ideas.md` also numbers a different idea `301`. Always use the namespaced id `JA-301`.

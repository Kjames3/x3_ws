---
id: JA-262
title: "RunLogger Incremental File Write for Repeat-Mode Memory Reduction"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "262"
session: "[2026-06-09] Automated Analysis Batch — Ideas 253 to 262"
tags: [idea]
---

# JA-262 — RunLogger Incremental File Write for Repeat-Mode Memory Reduction

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Script Optimization
**Investment:** Low
**Rationale:** In `repeat=True` mode, `RunLogger.log()` appends row dicts to `self.rows` in memory for the full leg duration, with `save()` called only at `ROTATE_HOME` completion. A 2-minute forward+return leg at 10 Hz accumulates ~1200 dict objects in memory, all written in a single burst. Opening the CSV file in `RunLogger.__init__`, writing the header immediately, and calling `writer.writerow(row)` in `log()` on each append eliminates peak memory, distributes I/O evenly across the leg, and prevents complete data loss if the process is killed or the robot is e-stopped mid-run.

---

## [2026-06-09] Automated Analysis Batch — Ideas 263 to 272

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `262`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `262` in its own log, but `June_performance_ideas.md` also numbers a different idea `262`. Always use the namespaced id `JA-262`.

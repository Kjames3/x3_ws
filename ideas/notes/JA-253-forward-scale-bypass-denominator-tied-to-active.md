---
id: JA-253
title: "Forward-Scale Bypass Denominator Tied to Active `BYPASS_OFFSET`"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "253"
session: "[2026-06-09] Automated Analysis Batch — Ideas 253 to 262"
tags: [idea]
---

# JA-253 — Forward-Scale Bypass Denominator Tied to Active `BYPASS_OFFSET`

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Collision Avoidance
**Investment:** Extremely Low
**Rationale:** In `DRIVE_TO_B` and `DRIVE_TO_A` (~lines 916–917 and 1108–1109 of `ab_comparison_test.py`), the diagonal bypass forward-speed blending uses `forward_scale = 1.0 - max(0.0, min(0.8, lateral_error / 0.5))` with `0.5` hardcoded. When Idea 110 dynamically reduces `BYPASS_OFFSET` to 0.10 m in tight corridors, `lateral_error` never exceeds 0.10 m, so `lateral_error / 0.5 = 0.20` maximum and `forward_scale` never falls below 0.84 — the diagonal speed reduction barely activates. Replacing the hardcoded `0.5` with `max(0.05, BYPASS_OFFSET)` makes the forward-speed blending proportional to the actual bypass target distance, ensuring smooth diagonal profiling whether the active offset is 0.10 m or 0.40 m.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `253`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `253` in its own log, but `June_performance_ideas.md` also numbers a different idea `253`. Always use the namespaced id `JA-253`.

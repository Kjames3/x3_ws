---
id: JA-191
title: "Speed-Adaptive Forward LiDAR Detection Range"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "191"
session: "[2026-06-08] Automated Analysis Batch — Ideas 187 to 198"
tags: [idea]
---

# JA-191 — Speed-Adaptive Forward LiDAR Detection Range

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Collision Avoidance
**Investment:** Low
**Rationale:** The front blockage check in `_update_bypass_offset` uses a fixed `x_fwd < 0.75m` threshold regardless of the robot's current commanded speed. At `MAX_LINEAR_SPEED = 0.20 m/s` with a `KP_DIST = 0.6` controller, the actual stopping distance can exceed 0.75 m. Scaling the forward detection window dynamically as `look_ahead = max(0.75, last_vx_cmd / KP_DIST + 0.30)` provides proportionally earlier obstacle detection at higher speeds, allowing the speed-scaling logic to begin braking before the robot is already inside the unsafe zone.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `191`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `191` in its own log, but `June_performance_ideas.md` also numbers a different idea `191`. Always use the namespaced id `JA-191`.

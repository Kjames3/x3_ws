---
id: JA-241
title: "ICP Computation Offloaded to a Dedicated Background Thread"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "241"
session: "[2026-06-09] Automated Analysis Batch — Ideas 241 to 252"
tags: [idea]
---

# JA-241 — ICP Computation Offloaded to a Dedicated Background Thread

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Navigation Accuracy
**Investment:** Medium
**Rationale:** `_scan_cb` in `ab_comparison_test.py` runs the full 3-iteration ICP (including `O(N²)` distance matrix construction) synchronously inside the ROS2 callback thread at ~8 Hz. This blocks the single-threaded ROS2 executor for the duration of each ICP run, potentially delaying `/odom` and `/scan` message processing and causing control loop jitter. Moving the ICP computation to a dedicated background thread — writing the accepted result into `self.corrected_x/y/yaw` under a lock — frees the scan callback to return immediately, eliminating executor starvation and scan queue buildup at the cost of one extra Python lock and thread.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `241`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `241` in its own log, but `June_performance_ideas.md` also numbers a different idea `241`. Always use the namespaced id `JA-241`.

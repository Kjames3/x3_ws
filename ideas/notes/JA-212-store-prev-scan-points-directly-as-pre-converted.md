---
id: JA-212
title: "Store `prev_scan_points` Directly as Pre-Converted NumPy Array"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "212"
session: "[2026-06-08] Automated Analysis Batch — Ideas 209 to 220"
tags: [idea]
---

# JA-212 — Store `prev_scan_points` Directly as Pre-Converted NumPy Array

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Navigation Accuracy
**Investment:** Extremely Low
**Rationale:** In `_scan_cb`, current scan points are built as a Python list of `(float, float)` tuples via `.append()` and then stored as `self.prev_scan_points = curr_pts`. In `_align_scans_icp`, the first operation is `P = np.array(prev_pts, dtype=np.float32)` — converting this list to a NumPy array on every ICP call at 20 Hz. Changing the storage to `self.prev_scan_points = np.array(curr_pts, dtype=np.float32) if curr_pts else np.empty((0, 2), dtype=np.float32)` eliminates the per-call `np.array()` allocation and copy from within the ICP function's hot path, requiring only a one-line change in `_scan_cb`.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `212`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `212` in its own log, but `June_performance_ideas.md` also numbers a different idea `212`. Always use the namespaced id `JA-212`.

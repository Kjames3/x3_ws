---
id: JA-096
title: "Precomputed Pinhole Projection Grid (Projection LUT)"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "96"
session: "[2026-06-06 07:00:00 -07:00] Iteration 24 Analysis"
tags: [idea]
---

# JA-096 — Precomputed Pinhole Projection Grid (Projection LUT)

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

* **Context:** In `_extract_depth_centroids`, pixel coordinates $(cx, cy)$ are projected to physical meters $(x_m, y_m)$ using division and multiplication operations in Python: `x_m = (cx - w / 2.0) * Z / fx`.
* **The Issue:** Performing float divisions and coordinate centering for multiple centroids in Python loops on every frame is inefficient.
* **Proposed Enhancement:**
  - Precompute two static float32 lookup tables `PX_LUT` and `PY_LUT` matching the dimensions of the downsampled depth frame: `PX_LUT[v, u] = (u - cx) / fx`.
  - Obtain physical coordinates instantly using index lookups and a single multiplication: `x_m = PX_LUT[cy, cx] * Z` and `y_m = PY_LUT[cy, cx] * Z`.
  - This removes all float division operations per frame, replacing them with fast array accesses.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `96`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `96` in its own log, but `June_performance_ideas.md` also numbers a different idea `96`. Always use the namespaced id `JA-096`.

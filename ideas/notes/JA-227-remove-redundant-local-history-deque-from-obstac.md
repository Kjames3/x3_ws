---
id: JA-227
title: "Remove Redundant Local `history` Deque from `ObstacleTracker`"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "227"
session: "[2026-06-08] Automated Analysis Batch — Ideas 221 to 230"
tags: [idea]
---

# JA-227 — Remove Redundant Local `history` Deque from `ObstacleTracker`

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Script Optimization
**Investment:** Low
**Rationale:** Each track in `ObstacleTracker` maintains two parallel `deque(maxlen=WINDOW_SIZE)` objects: `history` (local camera-frame coordinates) and `history_global` (global map-frame coordinates). In `_inference_loop`, `hist_local` is reconstructed entirely from `history_global` by applying the inverse robot rotation (lines 481–491); the `history` (local) deque is appended at tracking time but never read back during inference. Removing the `history` deque and its `.append()` call in `ObstacleTracker.update()` eliminates one deque allocation per new track, one append per 10 Hz update step per track (up to 5 tracks = 50 appends/second saved), and halves the per-track coordinate storage footprint with zero behavioral change.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `227`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `227` in its own log, but `June_performance_ideas.md` also numbers a different idea `227`. Always use the namespaced id `JA-227`.

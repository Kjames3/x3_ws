---
id: JP-097
title: "Export YOLO Model to TensorRT for Jetson Orin Hardware Acceleration"
status: Logged
source: ideas/June_performance_ideas.md
source_id: "97"
tags: [idea]
---

# JP-097 — Export YOLO Model to TensorRT for Jetson Orin Hardware Acceleration

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Target:** `src/server_x3.py`
**Idea:** The YOLOv11 model is run in PyTorch format (`.pt`), which performs inference on the GPU using generic PyTorch operations. Since the Jetson Orin has dedicated TensorRT acceleration cores, exporting the YOLO model to `.engine` format using `model.export(format='engine')` and loading the TensorRT engine will speed up detection frame times significantly, reducing latency and freeing up CPU cycles.

---

## Provenance

Generated from `ideas/June_performance_ideas.md` (heading `97`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `97` in its own log, but `June_architectural_ideas.md` also numbers a different idea `97`. Always use the namespaced id `JP-097`.

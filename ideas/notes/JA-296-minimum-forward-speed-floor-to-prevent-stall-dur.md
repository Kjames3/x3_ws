---
id: JA-296
title: "Minimum Forward Speed Floor to Prevent Stall During Active Lateral Bypass"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "296"
session: "[2026-06-09] Automated Analysis Batch — Ideas 293 to 302"
tags: [idea]
---

# JA-296 — Minimum Forward Speed Floor to Prevent Stall During Active Lateral Bypass

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Collision Avoidance
**Investment:** Extremely Low
**Rationale:** In both drive states, `speed = base_forward_speed * forward_scale` where `forward_scale = 1.0 - max(0.0, min(0.8, lateral_error / 0.5))`. When `lateral_error = 0.4 m`, `forward_scale = 0.2`, producing `speed = MIN_LINEAR_SPEED * 0.2 = 0.012 m/s` — below the mecanum wheel stall threshold. The robot halts forward progress while `lateral_error` remains large, creating a deadlock that can only be broken by `speed_scale ≤ 0.1`. Applying `speed = max(MIN_LINEAR_SPEED * 0.4, speed)` after the diagonal-blend line ensures at least 0.024 m/s of forward progress during lateral settling, allowing bypass completion via diagonal glide without freezing the control loop awaiting full lateral alignment.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `296`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `296` in its own log, but `June_performance_ideas.md` also numbers a different idea `296`. Always use the namespaced id `JA-296`.

---
id: JP-117
title: "Use `torch.inference_mode()` Instead of `torch.no_grad()` for Velocity Estimator"
status: Logged
source: ideas/June_performance_ideas.md
source_id: "117"
tags: [idea]
---

# JP-117 — Use `torch.inference_mode()` Instead of `torch.no_grad()` for Velocity Estimator

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Target:** `src/velocity_estimator.py`
**Idea:** During inference, the velocity estimator uses `with torch.no_grad():` to bypass gradient tracking. PyTorch's newer `torch.inference_mode()` is a highly optimized version that disables additional overhead like tracking version counters for views, yielding faster execution speeds and lower memory overhead compared to `no_grad`.

---

## Provenance

Generated from `ideas/June_performance_ideas.md` (heading `117`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `117` in its own log, but `June_architectural_ideas.md` also numbers a different idea `117`. Always use the namespaced id `JP-117`.

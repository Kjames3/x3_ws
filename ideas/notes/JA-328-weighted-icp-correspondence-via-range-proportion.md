---
id: JA-328
title: "Weighted ICP Correspondence via Range-Proportional Point Weights in `_align_scans_icp`"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "328"
session: "[2026-06-10] Automated Analysis Batch — Ideas 321 to 330"
tags: [idea]
---

# JA-328 — Weighted ICP Correspondence via Range-Proportional Point Weights in `_align_scans_icp`

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Navigation Accuracy
**Investment:** Low
**Rationale:** The current `_align_scans_icp` implementation treats all matched point pairs equally in the cross-covariance matrix `H = Q_bar.T @ P_bar`. Close-range LiDAR returns (< 1.5 m) have far lower angular uncertainty than far-range returns (> 3 m), so weighting each correspondence by `w_i = 1.0 / max(0.25, P_matched[i,0]**2 + P_matched[i,1]**2)` and computing `H = (Q_bar * w[:,np.newaxis]).T @ P_bar` reduces the influence of noisy far-wall correspondences (doorways, far-end walls) on the rotation solution, improving yaw correction accuracy in mixed near/far environments — a single `w` vector computation and one einsum replacement in the existing SVD-free 2D ICP, with no architecture change.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `328`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `328` in its own log, but `June_performance_ideas.md` also numbers a different idea `328`. Always use the namespaced id `JA-328`.

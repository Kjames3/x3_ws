---
id: JA-036
title: "Cropped Contour Masking for Centroid Extraction"
status: Implemented
source: ideas/June_architectural_ideas.md
source_id: "36"
session: "[2026-06-05 19:20:00 -07:00] Iteration 12 Analysis"
code_refs: [src/velocity_estimator.py]
tags: [idea]
---

# JA-036 — Cropped Contour Masking for Centroid Extraction

> [!success] Implemented
> - listed in June_roi_analysis.md → Already Implemented Ideas
> - referenced in src/velocity_estimator.py

* **Context:** In `_extract_depth_centroids`, to query raw depth values inside a contour, the code currently instantiates a full-sized mask (`np.zeros_like(mask)` of size 640x480) and draws a contour on it, then indexes `raw_depth_frame`.
* **The Issue:** Creating and processing full-sized image masks for multiple contours at 10Hz consumes unnecessary CPU cycles and memory bandwidth on the Jetson Orin.
* **Proposed Enhancement:**
  - Crop the bounding box of the contour: `x, y, w, h = cv2.boundingRect(cnt)`.
  - Allocate a small local mask of size $w \times h$ and draw the translated contour onto it.
  - Index the cropped slice of `raw_depth_frame[y:y+h, x:x+w]` using the local mask. This reduces memory allocations and pixel comparison cycles by over 95%.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `36`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `36` in its own log, but `June_performance_ideas.md` also numbers a different idea `36`. Always use the namespaced id `JA-036`.

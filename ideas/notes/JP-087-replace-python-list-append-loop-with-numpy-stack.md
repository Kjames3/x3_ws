---
id: JP-087
title: "Replace Python `list.append` Loop with NumPy `stack`"
status: Logged
source: ideas/June_performance_ideas.md
source_id: "87"
tags: [idea]
---

# JP-087 — Replace Python `list.append` Loop with NumPy `stack`

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Target:** `src/velocity_estimator.py`
**Idea:** The tracking history assembly builds feature vectors by appending to a Python list and then converting via `np.array(list)`. This creates N temporary Python float objects per append. Instead, pre-allocate a NumPy array of the correct shape and use index assignment (`features[i] = value`) to fill it directly, bypassing Python object creation entirely for faster feature assembly.

---

## Provenance

Generated from `ideas/June_performance_ideas.md` (heading `87`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `87` in its own log, but `June_architectural_ideas.md` also numbers a different idea `87`. Always use the namespaced id `JP-087`.

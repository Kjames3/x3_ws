---
id: JA-325
title: "Closest-Obstacle Track ID Column in `_maybe_log` for Post-Run Track Attribution"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "325"
session: "[2026-06-10] Automated Analysis Batch — Ideas 321 to 330"
tags: [idea]
---

# JA-325 — Closest-Obstacle Track ID Column in `_maybe_log` for Post-Run Track Attribution

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Navigation Accuracy
**Investment:** Extremely Low
**Rationale:** `_maybe_log` (lines 689–711 of `ab_comparison_test.py`) logs `n_obstacles` and `min_obstacle_dist`, but not which track ID corresponds to the minimum-distance obstacle. Adding `closest_id = min(estimates, key=lambda e: math.hypot(e.get('x',0), e.get('z',0)))['id'] if estimates else -1` as a `closest_obstacle_id` column enables post-run identification of whether each bypass event involved the same re-engaging pedestrian or a new track, and allows cross-run obstacle-engagement frequency analysis per mode that is currently impossible from distance-only logs.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `325`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `325` in its own log, but `June_performance_ideas.md` also numbers a different idea `325`. Always use the namespaced id `JA-325`.

---
id: JA-312
title: "O(N²) History Padding in `_build_window_features` Replaced with List Concatenation"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "312"
session: "[2026-06-09] Automated Analysis Batch — Ideas 311 to 320"
tags: [idea]
---

# JA-312 — O(N²) History Padding in `_build_window_features` Replaced with List Concatenation

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Script Optimization
**Investment:** Extremely Low
**Rationale:** The padding loop `while len(hist) < WINDOW_SIZE: hist.insert(0, hist[0] ...)` (lines 354–355 of `velocity_estimator.py`) calls Python `list.insert(0, x)` which is O(N) per call because it shifts all existing elements right. For a fresh track with 1 frame, 9 sequential `insert(0, ...)` calls perform 1+2+…+9 = 45 element-copy operations. Replacing with `hist = [hist[0]] * (WINDOW_SIZE - len(hist)) + hist` uses C-level list multiplication (O(WINDOW_SIZE) total) followed by one O(1) concatenation, making new-track feature assembly approximately 9× faster at 10 Hz with up to 5 simultaneous tracks.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `312`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `312` in its own log, but `June_performance_ideas.md` also numbers a different idea `312`. Always use the namespaced id `JA-312`.

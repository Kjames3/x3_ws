---
id: JA-206
title: "Static Fixture Tagging via EMA Motion Score to Clean Up TTC Loop"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "206"
session: "[2026-06-08] Automated Analysis Batch — Ideas 199 to 208"
tags: [idea]
---

# JA-206 — Static Fixture Tagging via EMA Motion Score to Clean Up TTC Loop

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Velocity Estimation
**Investment:** Low
**Rationale:** Tracks representing static furniture (chair legs, desk bases) appear regularly in depth images and accumulate high `visible_count` values, driving them through MLP inference every cycle. Unlike Idea 94 (which masks static objects in image space via LiDAR projection), maintain a lightweight per-track `motion_score = 0.8 * prev_score + 0.2 * (abs(dx) + abs(dy))` updated each frame in `ObstacleTracker.update()`. Tag any track with `motion_score < 0.004 m/frame` sustained for 20+ consecutive frames as `is_static_fixture` and skip it in `_get_speed_scaling` and bypass-initiation checks, without altering the depth extraction pipeline.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `206`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `206` in its own log, but `June_performance_ideas.md` also numbers a different idea `206`. Always use the namespaced id `JA-206`.

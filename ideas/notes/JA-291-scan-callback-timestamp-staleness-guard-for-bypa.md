---
id: JA-291
title: "Scan Callback Timestamp Staleness Guard for Bypass Logic"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "291"
session: "[2026-06-09] Automated Analysis Batch — Ideas 283 to 292"
tags: [idea]
---

# JA-291 — Scan Callback Timestamp Staleness Guard for Bypass Logic

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Collision Avoidance
**Investment:** Extremely Low
**Rationale:** `self.last_scan_ranges` in `ab_comparison_test.py` is initialized to `[]` and never tagged with a receive time. If the YDLidar driver restarts mid-run (USB disconnect, driver crash), `last_scan_ranges` retains its final valid snapshot while `_update_bypass_offset()` continues treating it as current data, potentially holding `is_paused = True` or `is_continuous_wall = True` indefinitely from stale readings. Adding `self._last_scan_time = time.monotonic()` in `_scan_cb` and guarding all LiDAR sector checks in `_update_bypass_offset` with `if time.monotonic() - self._last_scan_time > 1.0: skip_lidar` degrades gracefully to camera-only avoidance on sensor fault rather than freezing the robot.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `291`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `291` in its own log, but `June_performance_ideas.md` also numbers a different idea `291`. Always use the namespaced id `JA-291`.

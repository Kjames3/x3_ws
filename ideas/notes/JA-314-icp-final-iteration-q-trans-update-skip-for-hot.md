---
id: JA-314
title: "ICP Final-Iteration `Q_trans` Update Skip for Hot-Path Reduction"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "314"
session: "[2026-06-09] Automated Analysis Batch — Ideas 311 to 320"
tags: [idea]
---

# JA-314 — ICP Final-Iteration `Q_trans` Update Skip for Hot-Path Reduction

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Script Optimization
**Investment:** Extremely Low
**Rationale:** In `_align_scans_icp` (line 321 of `ab_comparison_test.py`), `Q_trans = (Q_trans @ R_iter.T) + t_iter` is executed unconditionally on all 3 iterations, but on the final (3rd) iteration `Q_trans` immediately falls out of scope and is never read again — only the accumulated `dtheta_corr`, `dx_corr`, `dy_corr` scalars are returned. Guarding the update with `if _ < 2:` inside the `for _ in range(3)` loop skips one (N×2) × (2×2) matrix multiply and one (N×2) broadcast add per scan callback at 20 Hz, eliminating roughly `4N` floating-point operations per call (≈1440 ops/second at N=360) at zero accuracy cost.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `314`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `314` in its own log, but `June_performance_ideas.md` also numbers a different idea `314`. Always use the namespaced id `JA-314`.

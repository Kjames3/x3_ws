---
id: JA-336
title: "ICP Cross-Covariance Matrix Ill-Conditioning Guard for Collinear Wall Scans"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "336"
session: "[2026-06-10] Automated Analysis Batch — Ideas 331 to 340"
tags: [idea]
---

# JA-336 — ICP Cross-Covariance Matrix Ill-Conditioning Guard for Collinear Wall Scans

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Navigation Accuracy
**Investment:** Extremely Low
**Rationale:** In `_align_scans_icp`, the rotation angle is extracted via `theta = arctan2(H[0,1] - H[1,0], H[0,0] + H[1,1])` from the cross-covariance matrix `H = Q_bar.T @ P_bar`. When the robot faces a single flat wall (all inlier points collinear along one axis), `H` becomes rank-1 with both diagonal terms near zero, causing `arctan2(large, ~0)` to produce ±π/2 spurious rotation corrections that corrupt `corrected_yaw` for the remainder of the run. Adding `if (H[0,0] + H[1,1])**2 + (H[0,1] - H[1,0])**2 < 1e-8: theta = 0.0` before the `arctan2` call detects near-singular H matrices and safely skips the rotation correction, preventing wall-facing degenerate scan alignments from injecting large yaw spikes at negligible runtime cost.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `336`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `336` in its own log, but `June_performance_ideas.md` also numbers a different idea `336`. Always use the namespaced id `JA-336`.

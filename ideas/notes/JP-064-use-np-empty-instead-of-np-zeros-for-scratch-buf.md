---
id: JP-064
title: "Use `np.empty` Instead of `np.zeros` for Scratch Buffers"
status: Logged
source: ideas/June_performance_ideas.md
source_id: "64"
tags: [idea]
---

# JP-064 — Use `np.empty` Instead of `np.zeros` for Scratch Buffers

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Target:** `src/velocity_estimator.py`
**Idea:** Several temporary arrays (masks, feature buffers) are allocated with `np.zeros()`, which writes zero to every byte. When these arrays are immediately overwritten by `cv2.drawContours` or filled with computed values, the initial zeroing is wasted work. Switching to `np.empty()` skips the initialization pass, saving memory bandwidth proportional to the array size.

---

**Date:** 2026-06-19 06:00 AM (Iteration 16)

---

## Provenance

Generated from `ideas/June_performance_ideas.md` (heading `64`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `64` in its own log, but `June_architectural_ideas.md` also numbers a different idea `64`. Always use the namespaced id `JP-064`.

---
id: JA-327
title: "Depth-Range Confidence Downscale for Far Tracks (Z > 2.5 m) in `_get_speed_scaling`"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "327"
session: "[2026-06-10] Automated Analysis Batch — Ideas 321 to 330"
tags: [idea]
---

# JA-327 — Depth-Range Confidence Downscale for Far Tracks (Z > 2.5 m) in `_get_speed_scaling`

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Velocity Estimation
**Investment:** Extremely Low
**Rationale:** Astra Pro depth accuracy degrades beyond 2.5 m; at 4 m, centroid depth jitter is ±15–20 cm per frame, which the MLP interprets as lateral motion and produces nonzero velocity predictions. In `_get_speed_scaling`, multiply each obstacle's TTC/proximity contribution by `depth_conf = max(0.2, 1.0 - max(0.0, cz - 2.5) / 1.5)` where `cz = est.get('z', 1.0)`. This softly discounts velocity predictions from far-range tracks (0.2× at 4 m) without disabling them, reducing false TTC braking from depth-noise-induced phantom motion at long range while preserving full sensitivity within 2.5 m where depth accuracy is reliable.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `327`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `327` in its own log, but `June_performance_ideas.md` also numbers a different idea `327`. Always use the namespaced id `JA-327`.

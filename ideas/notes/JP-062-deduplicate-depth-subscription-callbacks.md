---
id: JP-062
title: "Deduplicate Depth Subscription Callbacks"
status: Logged
source: ideas/June_performance_ideas.md
source_id: "62"
tags: [idea]
---

# JP-062 — Deduplicate Depth Subscription Callbacks

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Target:** `src/server_x3.py`
**Idea:** The server subscribes to both `/camera/depth_image` and `/camera/depth/image_raw` with the same `_depth_cb` callback. If both topics are active simultaneously (e.g., during Gazebo simulation), the callback processes depth frames at double the rate, wasting CPU. Add a timestamp guard (`if time.monotonic() - self._last_depth_write_time < 0.02: return`) at the top of `_depth_cb` to skip duplicate frames within 20ms.

---

## Provenance

Generated from `ideas/June_performance_ideas.md` (heading `62`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `62` in its own log, but `June_architectural_ideas.md` also numbers a different idea `62`. Always use the namespaced id `JP-062`.

---
id: JA-289
title: "Per-Track MLP Output Vector EMA for Stable Bypass Direction Commands"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "289"
session: "[2026-06-09] Automated Analysis Batch — Ideas 283 to 292"
tags: [idea]
---

# JA-289 — Per-Track MLP Output Vector EMA for Stable Bypass Direction Commands

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Velocity Estimation
**Investment:** Low
**Rationale:** The MLP outputs `(vx, vy)` per track at 10 Hz, and the sign of `vy` determines bypass direction preference in `_update_bypass_offset()` (lines 569–591). A single noisy frame where the predicted `vy` flips sign can reverse the bypass direction mid-maneuver. Storing `prev_vx` and `prev_vy` in `ObstacleTracker.tracks` at track creation and applying `vx_out = 0.6 * vx_pred + 0.4 * prev_vx` (similarly for `vy`) before writing to the estimates list provides stable bypass direction signals. This is distinct from Idea 238's scalar speed EMA; smoothing the full velocity vector prevents direction-flip artifacts that the scalar magnitude filter does not address.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `289`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `289` in its own log, but `June_performance_ideas.md` also numbers a different idea `289`. Always use the namespaced id `JA-289`.

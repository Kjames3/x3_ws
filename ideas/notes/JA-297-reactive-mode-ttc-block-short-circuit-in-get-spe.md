---
id: JA-297
title: "Reactive-Mode TTC Block Short-Circuit in `_get_speed_scaling`"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "297"
session: "[2026-06-09] Automated Analysis Batch — Ideas 293 to 302"
tags: [idea]
---

# JA-297 — Reactive-Mode TTC Block Short-Circuit in `_get_speed_scaling`

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Script Optimization
**Investment:** Extremely Low
**Rationale:** In `_get_speed_scaling()` (line 769 of `ab_comparison_test.py`), the `if self._mode == "predictive":` TTC guard is checked per-obstacle inside the loop, but in reactive mode `s_t` is always 1.0 — all TTC arithmetic (`math.hypot`, `r_dot_v`, conditional `ttc`) is dead computation. Since 50% of all A/B test runs use `--mode reactive`, this dead work runs at 20 Hz across all obstacle tracks for the entire reactive run. Hoisting the mode check outside the loop and using an early `return speed_scale` for reactive mode eliminates all TTC arithmetic per reactive tick, saving ~0.1–0.3 ms per 20 Hz call when multiple tracks are present.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `297`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `297` in its own log, but `June_performance_ideas.md` also numbers a different idea `297`. Always use the namespaced id `JA-297`.

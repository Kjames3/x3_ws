---
id: JP-073
title: "ONNX Runtime for Velocity MLP Instead of PyTorch"
status: Logged
source: ideas/June_performance_ideas.md
source_id: "73"
tags: [idea]
---

# JP-073 — ONNX Runtime for Velocity MLP Instead of PyTorch

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Target:** `src/velocity_estimator.py`
**Idea:** The pedestrian velocity MLP is loaded via `torch.jit.load()`, which pulls in the entire PyTorch runtime (~500MB in memory on ARM). Exporting the small MLP to ONNX format and running it through `onnxruntime` (which has a lightweight ARM build) reduces the runtime memory footprint by an order of magnitude and often yields faster inference for small models due to graph-level optimizations.

---

## Provenance

Generated from `ideas/June_performance_ideas.md` (heading `73`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `73` in its own log, but `June_architectural_ideas.md` also numbers a different idea `73`. Always use the namespaced id `JP-073`.

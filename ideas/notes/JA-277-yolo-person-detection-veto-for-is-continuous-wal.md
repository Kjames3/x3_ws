---
id: JA-277
title: "YOLO Person Detection Veto for `is_continuous_wall` Stationary Pedestrian Case"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "277"
session: "[2026-06-09] Automated Analysis Batch — Ideas 273 to 282"
tags: [idea]
---

# JA-277 — YOLO Person Detection Veto for `is_continuous_wall` Stationary Pedestrian Case

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Collision Avoidance
**Investment:** Low
**Rationale:** `is_continuous_wall` is set True when LiDAR shows blockage without range discontinuities and no `has_dynamic_pedestrian` (speed > 0.15 m/s). A stationary pedestrian standing directly in front presents a near-uniform LiDAR range profile (no edges) and has speed = 0.0, causing them to be misclassified as a static wall, suppressing camera-based bypass and halting the robot indefinitely. Adding a check against `self.detections_fn()` in the `is_continuous_wall` computation — if any YOLO `person` bounding box is present in the forward frustum, force `is_continuous_wall = False` regardless of edge count — correctly identifies stationary people as bypassable obstacles using the existing visual pipeline already available via `detections_fn`.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `277`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `277` in its own log, but `June_performance_ideas.md` also numbers a different idea `277`. Always use the namespaced id `JA-277`.

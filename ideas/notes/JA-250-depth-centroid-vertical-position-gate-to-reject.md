---
id: JA-250
title: "Depth Centroid Vertical Position Gate to Reject Floor-Level Ghost Detections"
status: Logged
source: ideas/June_architectural_ideas.md
source_id: "250"
session: "[2026-06-09] Automated Analysis Batch — Ideas 241 to 252"
tags: [idea]
---

# JA-250 — Depth Centroid Vertical Position Gate to Reject Floor-Level Ghost Detections

> [!note] Candidate — logged, not decided
> No implementation evidence found in the ROI analysis or in code comments.

**Area:** Velocity Estimation
**Investment:** Low
**Rationale:** In `_extract_depth_centroids` (raw depth path), `y_m = (cy - h / 2.0) * Z / fx` computes the vertical position of the centroid in metres relative to the camera center. Floor reflections, textured floor tiles, and low obstacles (door stoppers, cable runs) produce centroids at large positive `y_m` values (floor is below the camera, so cy > h/2). A simple gate `if y_m > 0.4 * Z: continue` (rejecting centroids that are more than 40% of their depth below camera centre height, approximately below ankle level) eliminates floor-level ghost detections without ground-plane RANSAC, reducing false TTC triggers and unnecessary tracking state from non-pedestrian floor artifacts.

---

## Provenance

Generated from `ideas/June_architectural_ideas.md` (heading `250`) by `scripts/split_ideas.py`. **Do not edit this file** — edit the source log and regenerate, or the two will drift.

> [!warning] Ambiguous legacy id
> This idea is `250` in its own log, but `June_performance_ideas.md` also numbers a different idea `250`. Always use the namespaced id `JA-250`.
